'use strict';

describe('myPhaserApp.tanks module', function() {

  beforeEach(module('myPhaserApp.tanks'));

  describe('tanks controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var tanksCtrl = $controller('TanksCtrl');
      expect(tanksCtrl).toBeDefined();
    }));

  });
});
