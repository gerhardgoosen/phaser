'use strict';

describe('myPhaserApp.breakout module', function() {

  beforeEach(module('myPhaserApp.breakout'));

  describe('breakout controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var breakoutCtrl = $controller('BreakoutCtrl');
      expect(breakoutCtrl).toBeDefined();
    }));

  });
});
