'use strict';

describe('myPhaserApp.invaders module', function() {

  beforeEach(module('myPhaserApp.invaders'));

  describe('invadersCtrl controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var invadersCtrl = $controller('InvadersCtrl');
      expect(invadersCtrl).toBeDefined();
    }));

  });
});
